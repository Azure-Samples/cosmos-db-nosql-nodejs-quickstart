# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: cosmos-db-nosql-go-quickstart
services:
  web:
    project: ./src
    language: js
    host: containerapp
    docker:
      path: ./Dockerfile
      context: ./
pipeline:
  provider: github
